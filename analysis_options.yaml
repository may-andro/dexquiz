include: package:flutter_lints/flutter.yaml

linter:
  rules:
    - avoid_renaming_method_parameters: false

analyzer:
  exclude:
    - bricks/**
    - lib/flavors/johto/firebase_options_johto.dart
    - lib/flavors/kanto/firebase_options_kanto.dart
  language:
    strict-casts: true
    strict-raw-types: true
