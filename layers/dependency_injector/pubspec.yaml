name: dependency_injector
description: Provides the dependency injection.
publish_to: 'none'

environment:
  sdk: '>=3.2.3 <4.0.0'

dependencies:
  get_it: ^7.6.4
  meta: ^1.10.0

dev_dependencies:
  lints: ^3.0.0
  test: ^1.24.3
  mocktail: ^1.0.2