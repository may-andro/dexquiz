name: firebase
description: A Flutter package to provide all firebase related dependencies.
publish_to: 'none'

environment:
  sdk: '>=3.1.2 <4.0.0'
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter
  firebase_analytics: ^10.4.5
  firebase_core: ^2.15.1
  firebase_crashlytics: ^3.3.5
  firebase_remote_config: ^4.2.5
  get_it: ^7.6.4

dev_dependencies:
  lints: ^3.0.0
  test: ^1.24.3
  mocktail: ^0.3.0
  build_runner: ^2.4.6