name: core
description: A Flutter package for core functionality of the app.
version: 0.0.1
publish_to: 'none'

environment:
  sdk: '>=3.1.2 <4.0.0'
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter
  firebase:
    path: ../firebase

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0