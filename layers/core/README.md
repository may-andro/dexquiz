This module has core logic needed by the main app.

## Features

### Core Entities

- [x] `BuildConfig`: Model class with basic configs to run the app.

## Usage

Import the module in the project in pubspec.yaml file.
```yaml
core:
  path: ../core
```